---
name: Terraform
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write

jobs:
  module-validation:
    uses: appvia/appvia-cicd-workflows/.github/workflows/terraform-module-validation.yml@c80bf314a4afac263cabe7dff37da8d26b416db3 # main
    name: Module Validation
    with:
      enable-terraform-tests: true
      working-directory: .
